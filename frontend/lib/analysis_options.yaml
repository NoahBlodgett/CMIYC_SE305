# Enable recommended Flutter lints
# See: https://pub.dev/packages/flutter_lints
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Add any project-specific rule overrides below, or remove this section.
    # example: prefer_single_quotes: true
    # prefer_single_quotes: true

analyzer:
  exclude:
    - build/**
    - .dart_tool/**
